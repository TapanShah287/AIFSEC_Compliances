name: Django CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      DJANGO_SETTINGS_MODULE: aif_compliance.settings
      SECRET_KEY: test-secret-key
      DEBUG: True
      DATABASE_URL: sqlite:///db.sqlite3

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install coverage

      - name: Show repo structure (debug)
        run: |
          pwd
          ls -R

      - name: Run migrations
        run: |
          python manage.py migrate --noinput

      - name: Run tests with coverage (explicit apps)
        run: |
          coverage run manage.py test admin api compliances dashboard docgen funds investee_companies investors manager_entities services transactions --pattern="tests.py"
          coverage report -m
